<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ignacio.liftlog">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LiftLog"
        tools:targetApi="31">

        <!-- Actividad principal -->
        <activity
            android:name=".screens.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividades reales (clases que heredan de ComponentActivity) -->
        <activity android:name=".screens.RegisterActivity" />
        <activity android:name=".screens.DashboardActivity" />
        <activity android:name=".screens.SettingsActivity" />
        <activity android:name=".screens.EntrenamientoDetalleActivity" />
        <activity android:name=".screens.RegistroEntrenamientoActivity" />
        <activity android:name=".screens.NutricionTabScreenActivity" />
        <activity android:name=".screens.SuenoTabScreenActivity" />
        <activity android:name=".screens.EstadisticasTabScreenActivity" />
        <activity android:name=".screens.ComunidadTabScreenActivity" />
    </application>
</manifest>



